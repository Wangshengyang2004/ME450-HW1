"""Creates a timer that runs at a fixed frequency based on a parameter called frequency that defaults to 100 Hz
Logs a DEBUG message from that timer that says "Issuing Command!" for each iteration of the timer
Once the timer is setup it should run until the node is terminated.
Do not attempt to stop the timer or create multiple timers
""" 